post:
  tags:
    - Echo
  summary: Echo test
  description: Receive the exact message you've sent
  operationId: echo
  security:
    - api_key: []
    - basic_auth: []
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: string
          examples:
            response:
              value: Hello world!
  requestBody:
    content:
      application/json:
        schema:
          type: string
          example: Hello world!
    description: Echo payload
    required: true